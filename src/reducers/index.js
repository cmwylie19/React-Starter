import { AuthProvider, AuthConsumer, useAuth, useAvatar } from './AuthReducer'
import { RouterProvider, useRouter, useHistory } from './RouterReducer'
import { ThemeProvider, ThemeConsumer, useTheme } from './ThemeReducer'

export {
    AuthProvider, 
    AuthConsumer,
    useAuth,
    useAvatar,
    RouterProvider,
    useRouter,
    useHistory,
    ThemeProvider,
    ThemeConsumer,
    useTheme
}